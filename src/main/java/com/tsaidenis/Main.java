package com.tsaidenis;

import com.tsaidenis.model.Categories;
import org.hibernate.Session;
import org.hibernate.Transaction;
import org.hibernate.cfg.Configuration;

public class Main {
    public static void main(String[] args) {
//        Categories categories = new Categories("Шорты",10L);
//        Categories categories2 = new Categories("Рубаха",11L);

//        Transaction transaction = null;

//        try(Session session = HibernateSessionFactoryUtil.getSessionFactory().openSession()){
//            transaction = session.beginTransaction();
//            session.persist(categories);
//            session.persist(categories2);
//            transaction.commit();
//        }

    }
}